OPENAI_API_KEY=sk-your-key-here
MODEL_CHEAP=gpt-4.1-mini
EMBED_MODEL=text-embedding-3-small
# Optional: add Langfuse keys to also ship the trace to Langfuse Cloud.
# Without them the exercise still runs; the OTel console exporter shows all.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
